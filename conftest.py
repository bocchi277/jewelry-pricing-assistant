# Presence of this file (even empty) makes pytest add the project root to
# sys.path, so `import calculator`, `import validator`, etc. work from
# files inside tests/ without any package/__init__.py gymnastics.
